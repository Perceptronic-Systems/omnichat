services:
  omnichat-backend:
    build: .
    container_name: omnichat-backend
    restart: unless-stopped
    environment:
      - OLLAMA_HOST=http://ollama-service:11434
    networks:
      - omnichat_net
    volumes:
      - /etc/omnichat/config.toml:/etc/omnichat/config.toml

  ollama:
    image: ollama/ollama:latest
    container_name: ollama-service
    restart: unless-stopped
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
    volumes:
      - ollama_data:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    networks:
      - omnichat_net

  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    restart: unless-stopped
    ports:
      - "443:443"
    volumes:
      - ./omnichat-nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/ssl/tailscale-certs:/etc/ssl/tailscale-certs:ro
    networks:
      - omnichat_net

volumes:
  ollama_data:

networks:
  omnichat_net:
    driver: bridge
    name: omnichat_net